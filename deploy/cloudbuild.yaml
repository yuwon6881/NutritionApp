# Supply an Artifact Registry image path. Deployment is a separate explicit step.
steps:
  - name: gcr.io/cloud-builders/docker
    args: ['build', '-t', '${_IMAGE}', '.']
images: ['${_IMAGE}']
substitutions:
  _IMAGE: asia-southeast1-docker.pkg.dev/REPLACE_PROJECT/nutrition/nourish:latest
