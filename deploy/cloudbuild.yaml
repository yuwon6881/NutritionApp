steps:
  - name: gcr.io/cloud-builders/docker
    entrypoint: bash
    args:
      - -c
      - |
        IMAGE="asia-southeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/nutrition-api"
        docker build -t "$$IMAGE:$COMMIT_SHA" -t "$$IMAGE:latest" .
        docker push "$$IMAGE:$COMMIT_SHA"
        docker push "$$IMAGE:latest"
    id: BuildPush

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - jobs
      - deploy
      - nutrition-migrate
      - --image=asia-southeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/nutrition-api:$COMMIT_SHA
      - --region=asia-southeast1
      - --service-account=nutrition-api@$PROJECT_ID.iam.gserviceaccount.com
      - --command=dotnet
      - --args=Nutrition.Api.dll,--migrate-only
      - --set-env-vars=PublicOrigin=${_PUBLIC_ORIGIN},Database__MigrateOnStartup=false,Identity__Authority=${_FITNESS_ACCOUNT_AUTHORITY},Identity__Issuer=${_FITNESS_ACCOUNT_ISSUER},Identity__NutritionAudience=nutrition-api,Identity__WorkoutAudience=workout-api,Integrations__KmsKeyName=${_INTEGRATION_KMS_KEY}
      - --set-secrets=ConnectionStrings__Database=nutrition-neon-database:latest,Identity__ClientSecret=fitness-nutrition-client-secret:latest
    id: Migrate
    waitFor: [BuildPush]

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args: [run, jobs, execute, nutrition-migrate, --region=asia-southeast1, --wait]
    id: MigrateExecute
    waitFor: [Migrate]

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - nutrition-api
      - --image=asia-southeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/nutrition-api:$COMMIT_SHA
      - --region=asia-southeast1
      - --platform=managed
      - --allow-unauthenticated
      - --min-instances=0
      - --max-instances=1
      - --memory=512Mi
      - --cpu=1
      - --timeout=180
      - --concurrency=20
      - --service-account=nutrition-api@$PROJECT_ID.iam.gserviceaccount.com
      - --set-env-vars=PublicOrigin=${_PUBLIC_ORIGIN},Auth__MaxUsers=2,Database__MigrateOnStartup=false,Identity__Authority=${_FITNESS_ACCOUNT_AUTHORITY},Identity__Issuer=${_FITNESS_ACCOUNT_ISSUER},Identity__ClientId=nutrition-api,Identity__RedirectUri=${_NUTRITION_REDIRECT_URI},Identity__ReturnUrl=/,Identity__ConnectReturnUrl=/settings,Identity__NutritionAudience=nutrition-api,Identity__WorkoutAudience=workout-api,Integrations__KmsKeyName=${_INTEGRATION_KMS_KEY},Integrations__WorkoutTrainingSummaryUrl=${_WORKOUT_SUMMARY_URL},OpenAi__Model=gpt-5.4-mini,Retention__MealDetailDays=90
      - --set-secrets=ConnectionStrings__Database=nutrition-neon-database:latest,Identity__ClientSecret=fitness-nutrition-client-secret:latest,OpenAi__ApiKey=financialapp-openai-api-key:latest,Cleanup__Token=nutrition-cleanup-token:latest
    id: Deploy
    waitFor: [MigrateExecute]

images: ['asia-southeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/nutrition-api:$COMMIT_SHA']
substitutions:
  _PUBLIC_ORIGIN: 'https://nutrition-diary-app.vercel.app'
  _FITNESS_ACCOUNT_AUTHORITY: 'https://fitness-account-i47taxhzba-as.a.run.app'
  _FITNESS_ACCOUNT_ISSUER: 'https://fitness-account-i47taxhzba-as.a.run.app'
  _NUTRITION_REDIRECT_URI: 'https://nutrition-diary-app.vercel.app/api/auth/central/callback'
  _WORKOUT_SUMMARY_URL: 'https://workout-api-i47taxhzba-as.a.run.app/api/integrations/v1/training-summary'
  _INTEGRATION_KMS_KEY: 'projects/example/locations/asia-southeast1/keyRings/fitness/cryptoKeys/integration-tokens'
